name: Playwright E2E Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Start WordPress Environment (wp-env)
      run: |
        npm install -g @wordpress/env
        wp-env start

    - name: Setup Configuration (Permalinks & Activation)
      run: |
        wp-env run cli wp rewrite structure '/%postname%/' --hard
        wp-env run cli wp plugin activate llms-txt-generator
        wp-env run cli wp rewrite flush --hard

    - name: Run Playwright tests
      run: npx playwright test
      env:
        WP_BASE_URL: http://localhost:8888

    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
